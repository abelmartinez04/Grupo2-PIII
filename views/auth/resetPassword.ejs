<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Restablecer contraseña | Farmacia La Salud</title>

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      background: linear-gradient(135deg, #28a745, #20c997);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    .card {
      max-width: 420px;
      width: 100%;
      border: none;
      border-radius: 15px;
      background: #fff;
      box-shadow: 0 4px 25px rgba(0, 0, 0, 0.15);
      padding: 30px;
    }

    .form-control {
      border-radius: 10px;
    }

    .btn-success {
      border-radius: 10px;
      background: linear-gradient(90deg, #28a745, #20c997);
      border: none;
      transition: all 0.3s ease;
    }

    .btn-success:hover {
      background: linear-gradient(90deg, #1e7e34, #159e74);
      transform: translateY(-1px);
    }

    .text-link {
      display: block;
      text-align: center;
      margin-top: 12px;
      text-decoration: none;
      color: #198754;
      font-weight: 500;
    }

    .text-link:hover {
      text-decoration: underline;
      color: #145c32;
    }

    .logo {
      text-align: center;
      font-size: 1.8rem;
      font-weight: bold;
      color: #198754;
      margin-bottom: 10px;
    }
  </style>
</head>

<body>
  <div class="card">
    <div class="logo">💊 Farmacia La Salud</div>
    <h4 class="text-center fw-bold mb-3">Restablecer contraseña</h4>
    <p class="text-center text-muted mb-4">
      Ingresa el código que recibiste por correo y tu nueva contraseña.
    </p>

    <form action="/resetPassword" method="POST">
      <input type="hidden" name="email" value="<%= email %>" />

      <div class="mb-3">
        <label for="code" class="form-label">Código recibido</label>
        <input
          type="text"
          name="code"
          id="code"
          class="form-control"
          placeholder="Ej: 123456"
          required
        />
      </div>

      <div class="mb-3">
        <label for="password" class="form-label">Nueva contraseña</label>
        <input
          type="password"
          name="password"
          id="password"
          class="form-control"
          placeholder="********"
          required
        />
      </div>

      <button type="submit" class="btn btn-success w-100">
        Actualizar contraseña
      </button>

      <a href="/login" class="text-link">← Volver al inicio</a>
    </form>

    <% if (message) { %>
      <div class="alert alert-info text-center mt-3 mb-0">
        <%= message %>
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
